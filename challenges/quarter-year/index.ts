/**
 * Quarter Year
 * 
 * Write a function that given a month as an integer from 1 - 12 returns which quarter of the year a month belongs to as an interger number 1 - 4.
 * return -1 is there is an error
*/

export const quarterYear = (input: number): number => {

    return;
}