/**
 * Case Checkrer
 * 
 * Write a function that checks if 2 given characters have the same case
 * A character will be a string of length 1 of a valid letter of the english alphabet
 * If there are any errors, return -1. E.G. character isn't a string, multiple characters given, etc
 * If both characters are letters and the same case, return 1
 * If both characters are letters, but not the same case, return 0
*/

export const caseChecker = (input1: string, input2: string): number => {
    
    return;
}