/**
 * Sheep Counter
 * 
 * Write a that takes an integer as an input and returns a string with repeating string of "N sheep..." where N is the current count of sheep up to the given integer.
 * E.G. Input: 3, Output: "1 sheep...2 sheep...3 sheep..."
 */

export const countSheep = (input: number): string => {
    return
}