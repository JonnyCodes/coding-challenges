/**
 * Football Score
 * 
 * Football scores are recorded in an array of strings, in the format "x:y", where x is our score and y is our opponents score.
 * 
 * Write a function that takes an array of football scores and returns the numbet if points our team has earned.

 * Points are awarded for each match as follows:
 * if x > y: 3 points (win)
 * if x < y: 0 points (loss)
 * if x = y: 1 point (tie)
 */

export const footballScore = (input: string[]): number => {

    return;
}